# Developer tools (not CI gates, but useful locally and for AI agents)

[tasks.blast-radius]
category = "TOOLS"
description = "Analyse changed files and recommend verification scopes"
command = "python3"
args = [".agents/skills/verify/scripts/blast_radius.py"]
